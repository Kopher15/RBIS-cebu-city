# Local PostGIS connection — copy this file to .env and fill in real values
# .env is gitignored and will NEVER be committed

DB_HOST=localhost
DB_PORT=5433
DB_NAME=gis_db
DB_USER=postgres
DB_PASSWORD=your_password_here
